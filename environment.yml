name: AI_GS
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10,<3.12
  - pandas>=2.0.0
  - numpy>=1.24.0
  - scikit-learn>=1.3.0
  - pyarrow>=12.0.0
  - pyyaml>=6.0
  - joblib>=1.3.0
  - openpyxl>=3.1.0
  - pip
  - pip:
    # Add any pip-only packages here if needed in the future

